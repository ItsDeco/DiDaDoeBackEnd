services:
  # Backend Service
  - type: web
    name: didadoe-backend
    env: node
    plan: free      # Explicitly set free plan
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: CLIENT_URL
        value: https://didadoe-frontend.onrender.com

  # Frontend Service  
  - type: web
    name: didadoe-frontend
    env: static
    plan: free      # Explicitly set free plan
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: ./client/dist
    envVars:
      - key: VITE_BACKEND_URL
        value: https://didadoe-backend.onrender.com